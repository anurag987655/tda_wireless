# Week Review

## Q1 What signal structure may create persistence loop?

A persistence loop is a hole which remains present througout multiple stage of filtration. In signal, generally periodic signal tends to create the persistence loop. And for persistence loop the value of persistence of $b-d$ is large. 

## Hypothesis for loop generating mechanism : 
A loop generating mechanism is a property of signal which causes its delay embedding transfomation to form closed curve. 

- A periodic signal may create a loop.
- A system that osciallates between states may generate loop.